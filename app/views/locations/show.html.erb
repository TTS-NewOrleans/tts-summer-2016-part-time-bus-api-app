<p id="notice"><%= notice %></p>

<h1>Your Get Away Bus...</h1>

<h3 class="text-muted">Your current location</h3>

<p class="text-muted">
  <%= @location.full_street_address %> <small><%= link_to fa_icon('pencil', text: 'Edit Your Location'), edit_location_path(@location) %></small>
</p>

<h3>The closest buses are...</h3>

<% @buses.each do |bus| %>
  <p>
    <strong>Route: </strong><%= bus["ROUTE"] %> <%= bus["DIRECTION"] %><br>
    <strong>Bus Number: </strong><%= bus["VEHICLE"] %><br>
    <strong>Next Stop: </strong><%= bus["TIMEPOINT"] %>
  </p>
<% end %>

<% if @buses.empty? %>
  <p>
    not really that close. Time to walk, bike, taxi, Uber, or <%= link_to 'change your location', edit_location_path(@location) %>
  </p>
<% end %>